export interface IDisabletime {
    startTime: string;
    endTime: string;
}
export interface IHour {
    hour: string;
    value: number;
}

export interface IMinute {
    munite: string;
    value: number;
}
